# Your application's domain. Use a wildcard like *.example.com for wildcard certificates.
APP_DOMAIN=test1.dev.tiatech.dev

# Email address for Let's Encrypt notifications.
SSL_EMAIL=contact@test1.dev.tiatech.dev

# Cloudflare API Token with Zone:DNS:Edit permissions for your domain.
# This is required for Caddy to perform the DNS-01 challenge.
CF_API_TOKEN=your_cloudflare_api_token_here
